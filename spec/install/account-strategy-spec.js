'use strict';

const AccountStrategy = require('../../lib/install/account-strategy');

describe('AccountStrategy', () => {
  let step;

  beforeEach(() => {
    step = new AccountStrategy();
  });
});
